<div class="container mt-5 p-1">
  <div class="w-75 mx-auto ">
    <% @articles.each do |article| %>
      <p class="fs-4"><a href="" class="bleue"><%= link_to article.title, article_path(article.id) %></a></p>
      <p class="fw-light txt-justify"><%= article.content[0..500] %></p>
    <% end %>
  </div>
</div>
